/**
 * OWNER: PAY
 * WHAT: Creates or inspects the Base Sepolia agent wallet and prints balances.
 * RUN:  pnpm wallet:fund
 * NOTE: Testnet only. Never point this at mainnet.
 */

async function main() {
  // print address, ETH balance, test-USDC balance, and the faucet URLs
  throw new Error("NOT_IMPLEMENTED: fund-wallet");
}

main().catch((e) => { console.error(e); process.exit(1); });


export {};
