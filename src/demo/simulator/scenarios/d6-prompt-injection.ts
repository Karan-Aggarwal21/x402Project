/**
 * OWNER: DEMO
 * ⭐ THE HERO SCENARIO. Search returns POISONED_RESULT; the agent obeys and tries 1000 x $2.
 * EXPECT: attempted $2,000.00, actual spend <= the daily budget, on-chain transactions from the
 *         attack: 0. Run it with the LLM driver on camera, deterministic driver as the fallback.
 */
export async function run() { throw new Error("NOT_IMPLEMENTED: D6"); }

