/** OWNER: CORE · ADMIN only. Triggers a FRESH evaluation, not a bypass. API_DOCS 5.4 */
import { notImplemented } from "@/shared/http";
export const POST = async () => notImplemented("POST /api/v1/approvals/:id/approve");

