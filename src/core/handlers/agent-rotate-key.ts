/** OWNER: CORE · returns the new plaintext key once · API_DOCS 5.1 */
import { notImplemented } from "@/shared/http";
export const POST = async () => notImplemented("POST /api/v1/agents/:id/rotate-key");

