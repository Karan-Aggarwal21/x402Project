/** OWNER: UI · Hour / day / month utilisation. Shows reserved separately from spent. */
export function BudgetGauge({ spent, budget }: { spent: string; budget: string }) {
  void spent; void budget;
  return <div>{/* TODO */}</div>;
}

