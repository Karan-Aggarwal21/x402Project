/** OWNER: UI · Transactions this minute vs the limit. Fills up during demo D3. */
export function VelocityMeter({ current, limit }: { current: number; limit: number }) {
  void current; void limit;
  return <div>{/* TODO */}</div>;
}

