/** OWNER: UI · Turns an error code into a sentence a judge understands. */
export function ReasonChip({ code, message }: { code: string; message?: string }) {
  void code; void message;
  return <span>{/* TODO */}</span>;
}

