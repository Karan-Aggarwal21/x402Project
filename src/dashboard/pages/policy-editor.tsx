/**
 * OWNER: UI · Route: /policies/[agentId]
 * DATA: GET/PUT /api/v1/policies/:agentId, GET .../versions
 * NOTE: saving creates a NEW immutable version. Show the diff the API returns.
 */
export function PolicyEditorPage() {
  return <div>{/* TODO: PolicyForm + JSON view + version history */}</div>;
}

