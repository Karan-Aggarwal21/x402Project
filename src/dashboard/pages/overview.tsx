/**
 * OWNER: UI · Route: /overview
 * SHOWS: spend today, decision counts, "money refused" tile, top block reasons, spend chart.
 * DATA: GET /api/v1/metrics/summary
 * NOTE: the "blocked on-chain transactions = 0" tile is goal G2 rendered as a number.
 */
export function OverviewPage() {
  return <div>{/* TODO: metric tiles + charts */}</div>;
}

