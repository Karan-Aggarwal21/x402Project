/**
 * OWNER: UI · Route: /transactions
 * DATA: GET /api/v1/transactions (filters: agent, decision, merchant, date)
 */
export function TransactionsPage() {
  return <div>{/* TODO: TxTable + summary block */}</div>;
}

