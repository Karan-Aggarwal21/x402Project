/**
 * OWNER: UI · Route: /agents/[agentId]
 * DATA: GET /api/v1/agents/:id, GET /api/v1/budgets/:agentId
 */
export function AgentDetailPage() {
  return <div>{/* TODO: wallet, budget gauges, velocity meter, recent decisions */}</div>;
}

